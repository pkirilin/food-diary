import React from 'react';
import { Typography } from '@material-ui/core';

const App: React.FC = () => {
  return (
    <div>
      <Typography variant="h1">App</Typography>
    </div>
  );
};

export default App;
