import { connect } from 'react-redux';
import PagesListControlPanel from './PagesListControlPanel';

const mapStateToProps = null;

const mapDispatchToProps = null;

export default connect(mapStateToProps, mapDispatchToProps)(PagesListControlPanel);
